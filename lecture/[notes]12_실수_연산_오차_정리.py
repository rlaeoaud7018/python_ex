"""
실수(Float) 연산의 오차 문제 완전 정리
=================================================

1. 실수 연산 오차란?
-------------------------------------------------
실수는 컴퓨터의 이진법 표현 방식 때문에
정확하게 저장될 수 없다.

특징:
- 모든 프로그래밍 언어에서 발생하는 현상
- 실수를 저장하는 순간부터 데이터가 왜곡됨
- 특히 부동소수점(Floating Point) 연산에서 발생

비유:
실수는 아날로그 신호처럼 정확한 표현이 불가능하다.
컴퓨터는 디지털 방식이므로 항상 근사치만 저장할 수 있다.
"""

result = 0.1 + 0.2

print(f'0.1 + 0.2 = {result}')
print(f'type: {type(result)}')

"""
위 코드 결과:
0.1 + 0.2 = 0.30000000000000004
type: <class 'float'>

예상 값: 0.3
실제 값: 0.30000000000000004

미세한 오차가 발생했다!


2. 왜 실수 오차가 발생하는가?
-------------------------------------------------
컴퓨터는 모든 데이터를 이진법(0과 1)으로 저장한다.

문제:
0.1을 이진법으로 정확하게 표현할 수 없다.

예:
0.1(십진법) = 0.0001100110011...(이진법, 무한 반복)

컴퓨터는 유한한 자리수만 저장하므로
자동으로 반올림하거나 올림하여 근사치를 저장한다.

결과:
여러 실수를 연산하면 오차가 누적된다.
"""

# 이진법으로 정확하게 표현되는 실수들
result1 = 0.5  # 1/2 → 이진법으로 정확히 표현 가능

print(f'0.5 = {result1}')

result2 = 0.25  # 1/4 → 이진법으로 정확히 표현 가능

print(f'0.25 = {result2}')

# 이진법으로 정확하게 표현되지 않는 실수들
result3 = 0.1  # 이진법으로 무한 반복

print(f'0.1 = {result3}')

result4 = 0.3  # 이진법으로 무한 반복

print(f'0.3 = {result4}')

"""
위 코드 결과:
0.5 = 0.5
0.25 = 0.25
0.1 = 0.1
0.3 = 0.3

출력상으로는 같아 보이지만,
내부적으로는 0.1과 0.3이 정확하지 않다.


3. 0.1 + 0.2 ≠ 0.3 문제
-------------------------------------------------
가장 유명한 부동소수점 오차 예제이다.
"""

num1 = 0.1
num2 = 0.2
expected = 0.3

result = num1 + num2

print(f'num1: {num1}')
print(f'num2: {num2}')
print(f'num1 + num2 = {result}')
print(f'예상 값: {expected}')
print(f'차이: {result - expected}')

"""
위 코드 결과:
num1: 0.1
num2: 0.2
num1 + num2 = 0.30000000000000004
예상 값: 0.3
차이: 5.551115123125783e-17

0.00000000000000005551... 의 미세한 오차가 발생했다!


4. 비교 연산자의 문제
-------------------------------------------------
실수 오차 때문에 == 연산자가 제대로 작동하지 않을 수 있다.
"""

total = 0.1 + 0.2

print(f'total: {total}')

# 잘못된 비교
if total == 0.3:
    print('total은 0.3과 같다')
else:
    print('total은 0.3과 다르다')

"""
위 코드 결과:
total: 0.30000000000000004
total은 0.3과 다르다

실제로는 0.3이어야 하지만, 비교 연산이 False를 반환한다!
"""

# 또 다른 예제
total = 0.1 + 0.2

print(f'total: {total}')
print(f'total > 0.3: {total > 0.3}')

if total > 0.3:
    print('total은 0.3보다 크다')

"""
위 코드 결과:
total: 0.30000000000000004
total > 0.3: True
total은 0.3보다 크다

오차 때문에 0.3보다 약간 크다!


5. 누적 오차 - 0.1씩 더하기
-------------------------------------------------
작은 오차도 누적되면 큰 오차가 된다.
"""

flt = 0.1
count = 0

print(f'0.1씩 더하기:')
print()

while True:
    count += 1
    print(f'반복 {count}: {flt}')
    
    if flt >= 1.0:
        break
    
    flt += 0.1
    
    if count >= 15:  # 무한 루프 방지
        print('...')
        break

"""
위 코드 결과:
0.1씩 더하기:

반복 1: 0.1
반복 2: 0.2
반복 3: 0.30000000000000004
반복 4: 0.4
반복 5: 0.5
반복 6: 0.6000000000000001
반복 7: 0.7000000000000001
반복 8: 0.8
반복 9: 0.9
반복 10: 0.9999999999999999
반복 11: 1.0999999999999999
...

0.1을 10번 더해서 1.0이 되어야 하는데,
누적된 오차로 인해 1.0999999999999999가 되었다!


6. 누적 오차의 원인
-------------------------------------------------
"""

# 각 단계별 오차 확인
step1 = 0.1
print(f'step 1: {step1:.20f}')

step2 = step1 + 0.1
print(f'step 2: {step2:.20f}')

step3 = step2 + 0.1
print(f'step 3: {step3:.20f}')

step4 = step3 + 0.1
print(f'step 4: {step4:.20f}')

step5 = step4 + 0.1
print(f'step 5: {step5:.20f}')

"""
위 코드 결과:
step 1: 0.10000000000000001
step 2: 0.20000000000000001
step 3: 0.30000000000000004
step 4: 0.40000000000000002
step 5: 0.50000000000000000

각 단계마다 미세한 오차가 발생하고,
이러한 오차들이 누적되고 있다.


7. 실수 오차 해결 방법 1 - round() 함수
-------------------------------------------------
round() 함수를 사용하여 지정한 소수점 자리로 반올림한다.
"""

total = 0.1 + 0.2

print(f'원본: {total}')

rounded_total = round(total, 1)

print(f'반올림: {rounded_total}')

# 비교
if rounded_total == 0.3:
    print('total은 0.3과 같다')
else:
    print('total은 0.3과 다르다')

"""
위 코드 결과:
원본: 0.30000000000000004
반올림: 0.3
total은 0.3과 같다

round() 함수로 소수점 1자리로 반올림하면 오차가 사라진다!
"""

# 누적 오차 해결
flt = 0.1
total_sum = 0

for i in range(10):
    total_sum += 0.1
    total_sum = round(total_sum, 1)

print(f'최종 합: {total_sum}')

"""
위 코드 결과:
최종 합: 1.0

매 단계마다 반올림하면 누적 오차를 방지할 수 있다.


8. 실수 오차 해결 방법 2 - 오차 범위 설정
-------------------------------------------------
== 비교 대신 오차 범위를 설정하여 비교한다.
"""

total = 0.1 + 0.2
expected = 0.3
epsilon = 1e-9  # 매우 작은 오차 허용치 (0.000000001)

print(f'total: {total}')
print(f'expected: {expected}')
print(f'차이: {abs(total - expected)}')
print(f'허용 오차: {epsilon}')
print()

# 틀린 방법
if total == expected:
    print('[== 연산] total은 expected와 같다')
else:
    print('[== 연산] total은 expected와 다르다')

# 올바른 방법
if abs(total - expected) < epsilon:
    print('[오차 범위 방법] total은 expected와 같다고 볼 수 있다')
else:
    print('[오차 범위 방법] total은 expected와 다르다')

"""
위 코드 결과:
total: 0.30000000000000004
expected: 0.3
차이: 5.551115123125783e-17
허용 오차: 1e-09

[== 연산] total은 expected와 다르다
[오차 범위 방법] total은 expected와 같다고 볼 수 있다

오차가 매우 작으면(epsilon보다 작으면) 같다고 판단한다.
"""

# 금융 계산 예제
price1 = 0.1
price2 = 0.2
total_price = price1 + price2

epsilon = 0.01  # 0.01원 이상의 차이만 오차로 인정

if abs(total_price - 0.3) < epsilon:
    print(f'총 가격: 0.3원 (오차 무시)')
else:
    print(f'총 가격: {total_price}원 (오차 발생)')

"""
위 코드 결과:
총 가격: 0.3원 (오차 무시)

epsilon을 0.01로 설정하면, 미세한 오차는 무시할 수 있다.


9. 실수 오차 해결 방법 3 - Decimal 모듈
-------------------------------------------------
decimal 모듈은 정확한 십진수 연산을 제공한다.
부동소수점 오차 없이 정확한 계산이 가능하다.
"""

from decimal import Decimal

# 일반 float
result_float = 0.1 + 0.2

print(f'float: 0.1 + 0.2 = {result_float}')

# Decimal
num1 = Decimal('0.1')
num2 = Decimal('0.2')
result_decimal = num1 + num2

print(f'Decimal: 0.1 + 0.2 = {result_decimal}')

"""
위 코드 결과:
float: 0.1 + 0.2 = 0.30000000000000004
Decimal: 0.1 + 0.2 = 0.3

Decimal을 사용하면 정확한 연산이 가능하다!
"""

# Decimal으로 누적 오차 해결
from decimal import Decimal

total = Decimal('0')

for i in range(10):
    total += Decimal('0.1')

print(f'Decimal 누적 합: {total}')

"""
위 코드 결과:
Decimal 누적 합: 1.0

완벽하게 1.0이 나온다!


10. 실수 오차가 발생하지 않는 경우
-------------------------------------------------
이진법으로 정확하게 표현되는 실수들이다.
"""

# 오차 없음
result1 = 0.5 + 0.25

print(f'0.5 + 0.25 = {result1}')

result2 = 0.25 + 0.75

print(f'0.25 + 0.75 = {result2}')

result3 = 0.125 + 0.875

print(f'0.125 + 0.875 = {result3}')

"""
위 코드 결과:
0.5 + 0.25 = 0.75
0.25 + 0.75 = 1.0
0.125 + 0.875 = 1.0

분모가 2의 거듭제곱(2, 4, 8, 16...)인 분수는
정확하게 표현된다.


11. 실무에서의 실수 오차 처리
-------------------------------------------------
"""

# 쇼핑몰 상품 가격 계산
item1_price = 19.99
item2_price = 29.99
item3_price = 14.99

total_price = item1_price + item2_price + item3_price

print(f'아이템 1: {item1_price}원')
print(f'아이템 2: {item2_price}원')
print(f'아이템 3: {item3_price}원')
print(f'총 가격 (오차 있음): {total_price}')
print(f'총 가격 (반올림): {round(total_price, 2)}원')

"""
위 코드 결과:
아이템 1: 19.99원
아이템 2: 29.99원
아이템 3: 14.99원
총 가격 (오차 있음): 64.96999999999999
총 가격 (반올림): 64.97원

금융 계산에서는 항상 round()를 사용하거나
Decimal을 사용해야 한다.


12. 실수 오차 테스트
-------------------------------------------------
"""

# 다양한 실수 연산 테스트
test_cases = [
    (0.1, 0.2, 0.3),
    (0.1, 0.1, 0.2),
    (0.2, 0.2, 0.4),
    (0.3, 0.3, 0.6),
]

print(f'실수 연산 오차 테스트:')
print()

for num1, num2, expected in test_cases:
    result = num1 + num2
    is_correct = (result == expected)
    
    print(f'{num1} + {num2} = {result}')
    print(f'예상값: {expected}')
    print(f'일치: {is_correct}')
    print()

"""
위 코드 결과:
실수 연산 오차 테스트:

0.1 + 0.2 = 0.30000000000000004
예상값: 0.3
일치: False

0.1 + 0.1 = 0.2
예상값: 0.2
일치: True

0.2 + 0.2 = 0.4
예상값: 0.4
일치: True

0.3 + 0.3 = 0.6000000000000001
예상값: 0.6
일치: False


=================================================
핵심 내용 총정리
=================================================

실수 오차 발생 원인:
- 컴퓨터는 모든 데이터를 이진법으로 저장
- 0.1, 0.2, 0.3 등은 이진법으로 정확히 표현 불가능
- 근사값만 저장되므로 오차 발생
- 연산할 때마다 오차가 누적됨

문제 사례:
- 0.1 + 0.2 ≠ 0.3
- 비교 연산자(==)가 제대로 작동하지 않음
- 금융 계산에서 큰 문제 발생 가능

해결 방법:

1. round() 함수 사용 (권장)
   round(value, 자리수)

2. 오차 범위 설정
   if abs(a - b) < epsilon:

3. Decimal 모듈 사용 (금융 계산 권장)
   from decimal import Decimal

우선순위:
- 금융 계산: Decimal 사용
- 일반 계산: round() 또는 오차 범위
- 게임/그래픽: 오차 범위 설정


=================================================
핵심 정리
=================================================

실수 오차는:
"컴퓨터의 이진법 표현 방식 때문에 필연적으로 발생하는 현상"

반드시 기억할 점:
1. 모든 프로그래밍 언어에서 발생한다
2. 실수를 저장하는 순간부터 오차가 시작된다
3. == 연산자로 실수를 비교하면 안 된다
4. 금융 계산은 Decimal을 사용해야 한다
5. 작은 오차도 누적되면 큰 오차가 된다

실제 개발에서의 사용:
- 금융 시스템 (Decimal 필수)
- 게임 개발 (오차 범위 설정)
- 과학 계산 (정확도 고려)
- 데이터 분석 (오차 인식)
- 좌표 계산 (오차 허용 범위 설정)

등에서 반드시 고려해야 하는 문제이다.
"""
